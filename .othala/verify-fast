#!/usr/bin/env bash
# Fast verification for PonziLand (no postgres required)
# This skips DB-dependent tests for quick iteration

set -euo pipefail

echo "[verify-fast] Starting fast verification (no DB)..."

# Fast check
echo "[verify-fast] Running cargo check..."
cargo check --all-targets

# Fast unit tests (skip DB tests)
echo "[verify-fast] Running unit tests (no db-tests feature)..."
cargo test --lib --no-default-features

echo "[verify-fast] ✓ Fast verification passed (DB tests skipped)"
